node_modules/
dist/

# Design references used to measure the layout. Kept out of the repo so their
# artwork is not redistributed; the measurements they produced live in
# src/lib/canvas/spec.ts.
reference/
*.local
.DS_Store
.vite/
*.tsbuildinfo

# The clip dev/audio-check.html records over. Drop any MP4 that has sound
# there; it stays out of the repo because someone else's clip is not this
# project's to redistribute.
dev/sample-clip.mp4
